const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../dropdown_menu';

describe('quotes.actions.dropdown_menu', () => {
  it('should do something');
});
