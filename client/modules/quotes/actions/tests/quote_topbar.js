const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../quote_topbar';

describe('quotes.actions.quote_topbar', () => {
  it('should do something');
});
