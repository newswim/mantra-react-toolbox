const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../quote_layout';

describe('quotes.actions.quote_layout', () => {
  it('should do something');
});
